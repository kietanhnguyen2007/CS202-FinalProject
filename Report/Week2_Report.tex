\documentclass[12pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{geometry}
\geometry{margin=1in}

\begin{document}

\begin{center}
    \LARGE \textbf{Weekly Report (Week 2)} \\
    \vspace{0.5cm}
\end{center}

\section*{General Information}
\begin{itemize}
    \item \textbf{Group ID:} 55
    \item \textbf{Project Name:} CS202 - Apple Knight
    \item \textbf{GitHub Repository:} \url{https://github.com/kietanhnguyen2007/CS202-FinalProject}
    \item \textbf{Date range:} 3/7/2026 - 10/7/2026
\end{itemize}

\section*{Detailed Tasks Completed This Week}
While our Markdown report provides a high-level summary of the week's progress, this document elaborates on the technical implementations and architectural decisions made by each member.

\subsection*{25125074 -- Nguyen Anh Kiet}
\begin{itemize}
    \item \textbf{Audio Assets Research \& Documentation:} Addressed the missing task from last week by thoroughly researching and compiling \texttt{SOUND\_ASSET\_GUIDE.md} and \texttt{DOWNLOAD\_SOUND\_ASSETS.md}. These documents map all game states, character classes, UI events, and elemental reactions to specific, verified free audio assets (Kenney, OpenGameArt).
    
    \item \textbf{Dynamic Sound Loading System:} Refactored \texttt{GameController::Init()} and implemented \texttt{LoadPlayerSounds(CharacterClass)} to dynamically load class-specific audio (Fighter, Knight, MagicCaster, Ninja) into generic internal keys (e.g., \texttt{player\_attack1}). This successfully eliminated hardcoded class-checking logic inside the combat loops, maintaining the engine's speed and cleanliness.
    
    \item \textbf{Backend \& UI Event Hookups:} Successfully integrated sound triggers into the core engine without altering existing physics or combat logic. Hooked up Player inputs (attacks, dash, ultimate), Enemy/Boss states, Pet actions (summon, dragon fire, ghost heal), and the \texttt{ElementalSystem} reactions (Vaporize, Conduct, Overload). Also added UI sound triggers for game states in \texttt{ResultView} and \texttt{MenuController}.
    
    \item \textbf{Repository Optimization:} Conducted a cleanup pass to remove multiple heavy, unused dummy \texttt{.wav} files from the repository. Updated \texttt{CMakeLists.txt} and \texttt{.gitignore} to manage audio assets effectively.
\end{itemize}

\subsection*{25125037 -- Nguyen Trong Tien}
\begin{itemize}
    \item \textbf{Enemy Combat Animations:} Developed and integrated robust attack animations for enemy entities within the main combat loop, modifying \texttt{Enemy.cpp} and \texttt{GameController.cpp} to ensure smooth visual feedback during monster strikes.
    
    \item \textbf{End Game Checkpoint Flag:} Implemented the final checkpoint logic and interaction flags. Updated \texttt{Item.h} and \texttt{Types.h} to properly trigger the end game sequence when the player successfully navigates the level and reaches the final goal.
    
    \item \textbf{Demo Game Polish \& Interaction (Carried over):} Finalized the playable demo level rendering, added specific attack combos (\texttt{attack1} on J, \texttt{attack2} on K, \texttt{attack3} on U) tailored for the Knight mode, and constrained Pet support mechanics (Ghost heal range and limits).
\end{itemize}

\section*{AI Usage Declaration}
\begin{itemize}
    \item \textbf{Debugging \& Constraint Enforcement:} AI was used to verify that inserting \texttt{PlaySound} calls into the high-frequency fixed-timestep loops (like \texttt{UpdateCombat} and \texttt{HandlePlayerInput}) did not introduce dynamic allocation or violate the project's strict Zero-Allocation rule.
    \item \textbf{Architectural Blueprints:} AI suggested the strategy of using dynamic search queries for OpenGameArt and Freesound instead of static links in the Asset Guide to completely eliminate the risk of 404 errors. It also provided the blueprint for mapping class-specific audio to generic keys to keep the controller logic decoupled.
\end{itemize}

\section*{Tasks Planned for Next Week}
\begin{itemize}
    \item Finalize downloading and importing all actual \texttt{.wav} and \texttt{.ogg} files based on the Asset Guide.
    \item Thoroughly test the audio system across the TCP network (ensuring Host and Client hear synchronized combat sounds).
    \item Continue refining boss AI patterns and dual-world level switching mechanics.
    \item Prepare the final project presentation and demo video.
\end{itemize}

\section*{Issues \& Resolutions}
\begin{itemize}
    \item \textbf{Issue 1:} Finding high-quality, free, and fitting sound assets for 4 distinct player classes and elemental reactions was highly time-consuming and prone to broken web links. \\
    \textbf{Resolution:} Utilized reliable open-source platforms (Kenney.nl) and created a dynamic search link strategy (\texttt{DOWNLOAD\_SOUND\_ASSETS.md}) to guarantee asset availability without 404 errors.
    
    \item \textbf{Issue 2:} The repository size was ballooning due to large, unused dummy \texttt{.wav} files. \\
    \textbf{Resolution:} Purged the unnecessary files and updated \texttt{.gitignore} to manage audio assets more effectively outside of direct source control tracking.
\end{itemize}

\end{document}
